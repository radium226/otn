# Oracle Resource Maven Plugin

Download and install resource (UCP, JDBC Driver, etc.) directly from Oracle website to local repository. 

```
mvn oracle-resource:install -Duser=... -Dpassword=... -Dresource=UCP
```
